global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'beta-swarm-core'
    static_configs:
      - targets: ['beta-swarm-core:8080']
