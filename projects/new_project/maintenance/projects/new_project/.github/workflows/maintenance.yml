name: Maintenance
on:
  push:
    branches:
      - main
jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run maintenance script
        run: ./maintenance.sh